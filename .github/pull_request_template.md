> Greenhouse spike — web-only / --no-checkout review. This branch contains autonomously generated
> code. Review in the GitHub web UI; do not check it out locally without inspecting .git-internal
> files first.
